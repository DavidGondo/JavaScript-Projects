Clicking a button {
when you click button,dispaly the content of the button on the screen
}

Computation{
if the previous value is a number and the succesive is an operator,expect the next to be a number
there can be no two consecutive operators
No number comes after an equal sign
if you press equal sign after number,compute the operation
}

let button=document.querySelector(".button");

btn.addEventListener("click",display());